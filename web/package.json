{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "esbuild src/main.ts --bundle --format=esm --target=es2022 --outfile=../app.js && cp src/style.css ../style.css",
    "test:parity": "node test/parity.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "esbuild": "^0.28.2"
  }
}
